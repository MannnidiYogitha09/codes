arr=list(map(int,input().split()))
k=int(input())
left=0
right=0
n=len(arr)
Sum=0
maxLen=0
while(right<n):
    Sum+=arr[right]
    while(Sum>k):
        Sum=arr[left]
        left+=1
    maxLen=max(maxLen,right-left+1)
    right+=1
print(maxLen)
